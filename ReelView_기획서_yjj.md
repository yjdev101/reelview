# ReelView 기획서

## 1. 프로젝트 개요

| 항목 | 내용 |
|------|------|
| 프로젝트명 | ReelView |
| 한 줄 소개 | 영화·드라마·애니메이션 작품별로 영상 리뷰를 모아볼 수 있는 영상 리뷰 플랫폼 |
| 형태 | Spring Boot REST API 서버 |
| 개발 목적 | Spring Boot 기반 웹 서버 개발 학습 |

## 2. 서비스 설명

왓챠·레터박스가 텍스트 리뷰를 모아주듯, ReelView는 **영상 리뷰**를 작품 단위로 모아주는 플랫폼입니다.

유튜브에 흩어진 영화·드라마·애니메이션 리뷰 영상들을 한 작품 페이지에서 한 번에 볼 수 있습니다.  
리뷰어는 직접 만든 영상을 업로드하거나, 유튜브 리뷰 링크를 등록해 큐레이션할 수 있습니다.

ReelView의 별점은 **작품이 아니라 리뷰 영상 자체**에 대한 평가입니다. 작품 평점은 왓챠·레터박스 등에 이미 충분히 존재하므로, ReelView는 "어떤 리뷰 영상이 가장 잘 만들어졌는지"를 다른 사용자들이 평가해 줄세우는 데 집중합니다.

### 기존 플랫폼과의 차별점

| 플랫폼 | 한계 |
|--------|------|
| 왓챠피디아, 레터박스 | 텍스트 리뷰만 지원 |
| 유튜브 | 영상 리뷰가 있지만 작품별로 모아볼 수 없음 |
| 네이버 영화 | 텍스트 기반, 영상 리뷰 없음 |

ReelView는 영상 플랫폼 시장에서 **"리뷰 영상"** 카테고리만 깊게 파고드는 **버티컬 서비스**입니다. 유튜브처럼 모든 영상을 다루는 수평적 플랫폼과 달리, 리뷰 영상 큐레이션에 집중해 해당 분야에서 압도적인 사용자 경험을 제공하는 것을 목표로 합니다.

## 3. 핵심 기능

| 기능 | 설명 |
|------|------|
| 회원가입 / 로그인 | 아이디·이메일·비밀번호 기반 인증. 권한은 USER(일반)/ADMIN(관리자)으로 구분 |
| 작품 등록 / 조회 | 영화·드라마·애니메이션 구분하여 작품 등록(여러 장르 태그 가능) 및 목록·상세 조회 |
| 장르 필터링 | 장르별로 작품 목록 조회 (작품 1개당 장르 여러 개 가능) |
| 리뷰 영상 등록 | 직접 영상 파일 업로드 **또는** 유튜브 URL 큐레이션 |
| 작품별 리뷰 목록 조회 | 특정 작품의 모든 리뷰 영상을 한 페이지에서 조회 |
| 리뷰 수정 / 삭제 | 제목, 설명 수정 및 삭제 |
| 리뷰 영상 평가 | 다른 사용자가 리뷰 영상에 별점(1~5)을 매겨 평가. 한 사용자가 같은 리뷰에 중복 평가 불가 |
| 정렬 | 최신순 / 평점순 (리뷰 영상 평가의 평균) |
| 댓글 요약 | URL 큐레이션된 유튜브 리뷰 영상의 댓글을 수집하여 AI로 요약 제공 |

## 4. 기술 스택

| 분류 | 기술 |
|------|------|
| Language | Java 21 |
| Framework | Spring Boot 3.x |
| DB | MySQL |
| ORM | Spring Data JPA |
| 빌드 도구 | Gradle |
| 외부 API | YouTube Data API v3, LLM API (Claude 등) |

## 5. 기술 스택 선정 이유

| 기술 | 선정 이유 |
|------|-----------|
| Java 21 | 현재 LTS 버전으로 안정적이며, Spring Boot 3.x가 요구하는 Java 17 이상 중 최신 LTS |
| Spring Boot 3.x | 스프링 설정을 자동화해 빠르게 서버를 구축할 수 있고, 내장 톰캣으로 별도 서버 설치 없이 실행 가능 |
| MySQL | 무료 오픈소스이며 국내에서 가장 널리 쓰이는 관계형 DB. 구조가 정해진 데이터를 저장하기에 적합 |
| Spring Data JPA | SQL을 직접 작성하지 않고 Java 코드로 DB를 다룰 수 있음. 기본 CRUD 메서드를 자동 제공해 반복 코드 감소 |
| Gradle | 프로젝트 빌드 및 의존성 관리 도구. Maven보다 문법이 간결하고 빌드 속도가 빠름 |
| YouTube Data API v3 | 유튜브 영상의 댓글 데이터를 공식 API를 통해 수집 |
| LLM API | 수집된 댓글을 AI로 요약. 대량의 텍스트를 간결하게 정리하는 데 적합 |

## 6. 기술적 도전 과제

### 6-1. Spring Boot 기초

| 과제 | 내용 | 해결 방법 |
|------|------|-----------|
| Spring Security 설정 | 로그인/인가 설정이 복잡하며, 설정 오류 시 모든 요청이 막히거나 전체가 열리는 문제 발생 | Security 설정을 단계적으로 적용하고, 각 설정이 어떤 역할인지 이해한 후 추가 |
| JPA 연관관계 매핑 | `user`, `content`, `review` 간 관계를 엔티티로 표현할 때 fetch type, cascade 설정 오류 발생 | `@ManyToOne`, `@OneToMany` 개념을 먼저 학습하고, 기본값(지연 로딩)을 이해한 후 적용 |
| N+1 쿼리 문제 | 작품 목록 조회 시 각 작품의 리뷰 데이터를 가져오려 하면 쿼리가 N번 추가 실행되는 성능 문제 | `JOIN FETCH` 또는 `@EntityGraph`로 연관 데이터를 한 번에 조회 |
| 엔티티 직접 반환 | JPA 엔티티를 API 응답으로 그대로 반환하면 비밀번호 등 민감 정보 노출, DB 구조가 API에 직접 노출 | 엔티티 대신 DTO(Data Transfer Object)를 만들어 필요한 필드만 반환 |
| jakarta 패키지 혼용 | Spring Boot 3.x는 `javax.*` 대신 `jakarta.*` 패키지를 사용. 구버전 예제 코드 복사 시 컴파일 오류 발생 | Spring Boot 3.x 기준 예제와 공식 문서를 사용하고, `jakarta.persistence.*` 임포트 확인 |
| HTTP 메서드 혼용 | GET/POST/PUT/DELETE를 용도에 맞지 않게 사용하면 REST 규칙에 어긋남 | 조회는 GET, 등록은 POST, 수정은 PUT/PATCH, 삭제는 DELETE로 명확히 구분 |
| 파일 저장 관리 | 영상 파일 저장 위치, 파일명 중복, 업로드 실패 시 임시 파일 처리 등 고려할 것이 많음 | UUID로 파일명을 고유하게 생성하고, 저장 경로를 설정 파일로 분리해 관리 |

### 6-2. 외부 API 연동

| 과제 | 내용 | 해결 방법 |
|------|------|-----------|
| 대용량 영상 업로드 | Spring Boot 기본 설정은 파일을 메모리에 통째로 올림 | 스트리밍 방식으로 처리하도록 설정 변경 (`file-size-threshold` 조정), 로컬 임시 파일 저장 후 처리 |
| YouTube API 할당량 | 기본 10,000 유닛/일 제한. 댓글 대량 수집 시 빠르게 소진 | 댓글 수집을 요청 시점에 즉시 호출하지 않고 필요할 때만 호출, 수집 결과를 DB에 캐싱해 재호출 방지 |
| 댓글 요약 토큰 제한 | 댓글이 많으면 LLM의 토큰 한도 초과 가능 | 댓글을 일정 개수(예: 100개)로 제한하거나 청킹(분할 처리) 후 요약 결과를 합산 |
| 외부 API 이중 연동 | YouTube API와 LLM API를 동시에 연동하는 복잡도 증가 | 각 API 연동을 독립적인 서비스 클래스로 분리해 관리, 오류 발생 시 각각 독립적으로 처리 |
