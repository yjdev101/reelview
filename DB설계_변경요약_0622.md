# ReelView DB/ERD 설계 변경 요약 (2026-06-22)

기존 `DB설계.md` 대비 오늘 바뀐 핵심 결정 3가지 정리.

---

## 1. 장르: 단일 컬럼 → N:M

- **기존**: `content.genre` VARCHAR 단일 컬럼 (작품당 장르 1개만 가능)
- **변경**: `genre` 테이블 + `content_genre` 매핑 테이블로 분리 (N:M)
- **이유**: 실제 영화·드라마는 장르가 보통 2개 이상 (예: 인셉션 = 액션+SF+스릴러). 단일 컬럼으로는 표현 불가능했음.

## 2. FK 삭제 정책(ON DELETE) 확정

| FK | 정책 | 이유 |
|----|------|------|
| review.user_id → user.id | SET NULL | 회원 탈퇴해도 리뷰는 다른 사용자에게 가치 있는 콘텐츠라 보존 |
| review.content_id → content.id | RESTRICT | 작품은 마스터 데이터, 리뷰가 있으면 삭제 금지 |
| content_genre.content_id → content.id | CASCADE | 작품 삭제 시 매핑도 함께 삭제 |
| content_genre.genre_id → genre.id | RESTRICT | 사용 중인 장르는 삭제 방지 |
| review_rating.review_id → review.id | CASCADE | 리뷰 삭제 시 그 평가들도 함께 삭제 |
| review_rating.user_id → user.id | CASCADE | 탈퇴 시 그 사람이 남긴 평가(투표 1건)도 삭제, 평점은 재계산 |

## 3. 핵심 피벗 — 평점은 "작품"이 아니라 "리뷰 영상"에 대한 평가

- **기존**: `review.rating` 컬럼 → 리뷰 작성자가 작품에 매기는 평점 (왓챠/레터박스드 방식)
- **변경**: `rating`을 `review_rating` 별도 테이블로 분리 → 작품이 아니라 **2차 콘텐츠인 리뷰 영상 자체**를 다른 사용자들이 평가
- **이유**: ReelView의 본질은 "양질의 리뷰 영상을 모아서 보고, 그 영상을 평가"하는 것. 작품 자체 평점은 왓챠 등에 이미 충분히 존재해 의도적으로 다루지 않음.
- **구조 변화**: `user`-`review`가 작성(1:N) 관계 외에 평가(N:M, `review_rating` 매핑 테이블) 관계도 추가됨
- **부가 효과**: "유튜브로 충분하지 않냐"는 피드백에 대한 보완이 될 수 있는 지점. 유튜브엔 "작품별로 모은 리뷰 영상들을 평점으로 줄세워 보여주는" 기능이 없음.

## 4. 최종 테이블 구조 (6개)

`USER` · `CONTENT` · `GENRE` · `CONTENT_GENRE` · `REVIEW` · `REVIEW_RATING`

자세한 컬럼/관계는 `DB설계.md`, `ERD.md` 참고.

## 5. 구현 계획 (단계적)

학습 목적 프로젝트라 한 번에 다 만들지 않고 단계적으로 구현 예정.

1. **1단계**: USER / CONTENT / REVIEW — 회원가입·로그인, 작품 등록, 리뷰 등록/조회 (핵심 CRUD)
2. **2단계**: GENRE / CONTENT_GENRE — 장르 등록·필터링
3. **3단계**: REVIEW_RATING — 리뷰 영상 평가, 평점순 정렬
